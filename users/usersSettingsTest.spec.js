// const request = require('supertest');

// const server = require('../index');

// const userUpdate = {
//   weight: 200,
//   gender: 'female',
//   height: 200,
// };

// describe('PUT/settings/:id', () => {
//   it('Should response with 200 status code update user table and return json object containing userSettings  in full', async () => {
//     const res = await request(server).put('/settings/1').send(userUpdate);
//     expect(res.status).toBe(200);
//     expect(res.type).toBe('application/json');
//     expect(res.body).toBeInstanceOf(Object);
//   });
// });
